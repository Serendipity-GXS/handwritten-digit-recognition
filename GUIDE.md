# 手写数字识别 CNN（纯 C 实现）· 学习与开发指导文档

> **读者对象：** 本文档写给两方——
> 1. **VSCode 侧边栏的 Claude 助手**：这是你的"项目说明书 + 带教手册"。每个会话开始时先读本文档，明确小郭当前所处阶段、要教的知识、要写的代码、验收标准，再开始指导。
> 2. **小郭本人**：这是你的完整学习路线图，可以随时翻开对照进度。

> **一句话项目定位：** 用**纯 C**（C11、零第三方库）从零实现一个**可训练、可推理的 CNN**，识别 MNIST 手写数字 0–9，并能在训练后读取一张手写图片输出预测结果。

---

## 目录

- [0. 本文档怎么用](#0-本文档怎么用)
- [1. 项目定位与边界](#1-项目定位与边界)
- [2. 学习者画像与协作守则](#2-学习者画像与协作守则)
- [3. 总体路线图（鸟瞰）](#3-总体路线图鸟瞰)
- [4. 知识体系地图（概念与推导清单）](#4-知识体系地图概念与推导清单)
- [5. 分阶段实施细节](#5-分阶段实施细节)
- [6. 侧边栏 Claude 指导守则](#6-侧边栏-claude-指导守则)
- [7. 常见坑与调试工具箱](#7-常见坑与调试工具箱)
- [8. 参考资料](#8-参考资料)
- [附录 A：项目目录结构与技术决策](#附录-a项目目录结构与技术决策)
- [附录 B：进度打卡表](#附录-b进度打卡表)

---

## 0. 本文档怎么用

**给侧边栏 Claude：**
1. 每个新会话开始，先读 `GUIDE.md`（和当前 `src/` 下的代码）。
2. 用「进度打卡表」（附录 B）确认小郭现在做到哪一步，**先验收上一阶段**，再开新阶段。
3. 按第 6 节的指导守则带教：**一次只讲一个概念、先讲思路再给代码、每个新层必须过数值梯度检查**。

**给小郭：**
- 按第 3 节的阶段顺序走，**不要跳阶段**。第 3 阶段（MLP 热身）是为第 4 阶段（CNN）打地基，跳过去后面会很难。
- 每个阶段的「验收标准」就是"过关"信号，达标了再继续。
- 卡住了先告诉 Claude"我卡在 X，XX 概念没懂"，别硬撑。

---

## 1. 项目定位与边界

| 维度 | 说明 |
|---|---|
| **目标** | 用纯 C 实现一个可训练、可推理的 CNN，在 MNIST 手写数字识别上达到 ~97%+ 准确率；并提供一个"读图 → 预测"的演示程序 |
| **技术边界** | 纯 C（C11）、只用标准库（`stdio/stdlib/math/string` 等）、**不使用任何 ML/矩阵第三方库**，矩阵运算、卷积、反向传播全部手写 |
| **运行环境** | Windows 桌面 + VSCode + MinGW-w64 GCC（见阶段 0）；构建用简单 Makefile |
| **最终形态** | 命令行程序 `main.exe`，支持 `train` / `eval` / `demo` 三种模式 |
| **设计原则** | 纯标准 C、显式 `malloc/free`、无平台特定代码、内存布局清晰可移植——**为未来移植嵌入式（如 STM32）预留可能** |

### 侧边栏 Claude 的三重角色

1. **代码编写辅助**：帮小郭把"学会了"的思路落成 C 代码，纠错、优化、讲解每一行的作用。
2. **知识系统讲解**：小郭是深度学习 0 基础，所有概念需要你**从直觉开始、系统化**地讲。
3. **公式推导**：每个反向传播公式都要**从链式法则一步步推**，并配小例子（2×2 / 3×3）手算核对，不能只给结论。

---

## 2. 学习者画像与协作守则

### 2.1 小郭的背景

- **身份**：电子硬件工程师，主用 C，常用工具链 Keil / IAR / GCC-ARM（熟悉 MCU/嵌入式开发）。
- **已有基础**：微积分（导数、链式法则没问题）、线性代数（矩阵运算有概念）、少量数据结构。
- **待补**：机器学习基本概念、神经网络、反向传播、CNN——**几乎全部深度学习知识**。
- **学习特点**：工程思维强、动手型，喜欢"看得见、能运行"的验证方式；需要从已知（C 代码、数学）桥接到未知（深度学习）。

### 2.2 协作守则（Claude 必须遵守）

1. **从已知到未知**：每个新概念尽量从小郭已有的 C 语言 / 数学经验出发（例："卷积窗口滑动"≈ 图像处理里的滤波；"张量"≈ 多维数组）。
2. **先直觉、后公式、再代码**：三步走模板见 §6.2。别一上来甩公式或代码。
3. **一次只讲一个概念**：宁慢勿快，控制单次讲解篇幅，讲完确认小郭复述/回答，再继续。
4. **每个反向传播层必须过数值梯度检查**（见 §5.3.6）再进下一层——这是本项目最重要的防呆机制。
5. **沟通用中文**；**代码和变量名用英文**；重要改动先出方案再动手。
6. **别替小郭思考**：用提问引导（"这一步的梯度形状应该是什么？"），等他 30 秒，再给提示或答案。
7. **明确验收再前进**：每阶段对照验收标准，达标才进入下一阶段。

---

## 3. 总体路线图（鸟瞰）

| 阶段 | 内容 | 里程碑（验收标准） | 学会的关键知识 |
|---|---|---|---|
| **0** | 环境搭建 + 数据集获取 | 能编译运行一个 hello 程序；MNIST 数据文件就位且大小正确 | 工具链、项目结构、idx 格式概念 |
| **1** | 数据层 `data.c/h` | 程序能打印出**可辨认的数字字符画** | 二进制读取、大端字节序、归一化 |
| **2** | 张量工具层 `tensor.c/h` | `Tensor` 结构体 + 索引/运算自测通过 | 张量、内存布局（行优先） |
| **3** | **MLP 热身网络** | 测试集准确率 **~92%**；loss 曲线可见下降 | 前向传播、反向传播、SGD、交叉熵、softmax |
| **4** | **CNN** | 测试集准确率 **~97%+**；能打印卷积层特征图 | 卷积、池化及各自反向传播、感受野、特征图 |
| **5** | 工程化 + 图片识别演示 | 能保存/加载权重；`demo` 模式读图预测正确并输出置信度 | 序列化、推理管线、混淆矩阵 |
| **6（可选）** | 性能优化 / 嵌入式延伸 | 按需展开 | im2col、OpenMP、量化、移植 STM32 |

> **为什么必须先做 MLP（阶段 3）再上 CNN（阶段 4）？**
> CNN 与 MLP 共享同一套"前向 → 反向 → 更新 → 训练循环"的骨架。先用 MLP 把**最难、最核心**的链式法则反向传播学透（全连接层公式直观、容易手算验证），CNN 就只剩下"卷积层、池化层各自的梯度怎么算"这两个**新增**知识点。一次只处理一个新困难，0 基础最稳。
> 而且 MLP 的训练框架（网络组装、SGD、循环、打印指标）在阶段 4 **几乎全部复用**。

---

## 4. 知识体系地图（概念与推导清单）

> 这张表是 Claude 的"教学内容大纲"。标 **【推】** 的，必须配完整公式推导 + 数值小例子；标 【讲】 的，讲清直觉即可。

### 4.1 数学与基础（阶段 1–2）

- 【讲】 点积与矩阵乘法（`C` 中的数组实现，对应 `A[i][k]*B[k][j]`）
- 【讲】 偏导数、复合函数求导（**链式法则**——后面反向传播的地基）
- 【讲】 张量 = 多维数组；行优先连续内存布局；为什么 CNN 需要 4D 张量 `(N, C, H, W)`

### 4.2 机器学习基础（阶段 3 前）

- 【讲】 监督学习：训练集 / 测试集、样本、标签
- 【讲】 泛化与过拟合（记下来就好，阶段 3 末尾会遇到）
- 【讲】 为什么把数据归一化到 [0,1]（数值稳定、梯度好走）

### 4.3 神经网络核心（阶段 3）

- 【推】 单个神经元 = 线性回归：`ŷ = wx + b`，损失用均方误差，梯度下降更新
- 【推】 激活函数及导数：ReLU（`relu'(z)=1 if z>0 else 0`）、Softmax（`∂p_i/∂z_j = p_i(δ_ij − p_j)`）
- 【推】 多分类损失：**交叉熵** `L = −Σᵢ yᵢ·log pᵢ`，并推导为什么分类不用 MSE
- 【推】 **softmax + 交叉熵的联合梯度**：`∂L/∂zᵢ = pᵢ − yᵢ`（经典且优雅，必推）
- 【推】 **全连接层反向传播**：
  - 前向：`z = W·x + b`，`a = f(z)`
  - 对输入梯度：`δ_prev = Wᵀ · δ`（逐元素乘 `f'(z)`）
  - 对权重梯度：`dW = δ · xᵀ`，对偏置：`db = δ`
  - 逐步推导链式法则怎么把误差传回上一层
- 【讲】 参数初始化（小随机数，为什么不能全 0 或太大）
- 【讲】 SGD 与学习率：`w ← w − η·dW`；为什么用**小批量**（batch）
- 【推】 **数值梯度检查**：`df/dx ≈ (f(x+ε) − f(x−ε)) / 2ε`，相对误差 < 1e-4（float）判定通过

### 4.4 CNN（阶段 4）

- 【讲】 为什么全连接不够：图片的空间结构被压平，参数量爆炸
- 【讲】 卷积 = 滑动窗口 + **权值共享**（从小郭熟悉的"图像滤波/边缘检测"切入）；**"卷积"实际是相关**（核不翻转），叫卷积是历史原因
- 【讲】 stride / padding 与输出尺寸公式：`out = (H − K) / stride + 1`
- 【讲】 特征图、多通道（输入通道 × 输出通道）、感受野
- 【讲】 最大池化：降采样 + 平移不变性；为什么用 max 不用平均（直觉）
- 【推】 **卷积层反向传播（两个方向）**：
  - 对权重梯度：`dW = input 与 δ 的 valid 相关`
  - 对输入梯度：`dInput = δ 与翻转后的核的 full 相关`
  - 用 3×3 输入 + 3×3 核的极小例子手算验证
- 【推】 **最大池化反向传播**：梯度只回传给池化窗口内**最大值所在位置**（其余位置为 0）

### 4.5 工程化（阶段 5）

- 【讲】 权重序列化：自定义二进制格式（magic + 各层 shape + 数据）
- 【讲】 推理与训练的代码分离；top-1 / top-3 与置信度
- 【讲】 混淆矩阵怎么读

---

## 5. 分阶段实施细节

> 每个阶段给出：**目标 · 概念讲解点 · 要写的文件与函数 · 验收标准**。Claude 每次带教时对照执行。

### 阶段 0：环境搭建与数据集

**目标**：能编译运行 C 程序；MNIST 数据文件就位。

**概念讲解点**：
- （讲） idx 文件格式预览：魔数、维度、大端（详细到阶段 1 再展开）
- （讲） 为什么选 MinGW-w64 GCC：免费、与 VSCode 集成好、和小郭熟悉的 GCC-ARM 一脉相承

**要做的**：
1. 安装 **MinGW-w64 GCC**（推荐 `winget install -e --id BrechtSanders.WinLibs.POSIX.UCRT`，或用 MSYS2 / winlibs.com 下载安装包），确认 `gcc --version` 可用。
2. VSCode 安装 **C/C++ 扩展**（Microsoft 官方）。可选配置 `tasks.json`/`launch.json` 做一键编译+F5 调试。
3. 按附录 A 建立项目目录结构。
4. **下载 MNIST 数据集**（4 个文件，放到 `data/`）：
   - **推荐：直接运行项目脚本**（一键下载 + 解压 + 大小校验）：
     `powershell -ExecutionPolicy Bypass -File scripts/download_mnist.ps1`
   - 脚本可选源：`-Source googleapis`（默认，国内实测 2MB/s+）/ `s3` / `official`
   - 各源 URL：官方 `http://yann.lecun.com/exdb/mnist/`；Google 镜像 `https://storage.googleapis.com/cvdf-datasets/mnist/`（国内推荐）；S3 镜像 `https://ossci-datasets.s3.amazonaws.com/mnist/`（国内很慢）
   - 文件名与**解压后大小校验值**：

   | 文件 | 内容 | 解压后大小 |
   |---|---|---|
   | `train-images-idx3-ubyte.gz` | 60000 张训练图 | 47,040,016 字节（≈45MB） |
   | `train-labels-idx1-ubyte.gz` | 60000 个训练标签 | 60,008 字节 |
   | `t10k-images-idx3-ubyte.gz` | 10000 张测试图 | 7,840,016 字节 |
   | `t10k-labels-idx1-ubyte.gz` | 10000 个测试标签 | 10,008 字节 |
   - 脚本自动解压（.gz → .idx，Windows 内置 GzipStream）并用上表大小校验；全部通过会打印 `ALL FILES VERIFIED.`
   - 手动校验：`Get-ChildItem data` 或 `ls -l data` 对照上表大小。

**验收标准**：
- [ ] `gcc --version` 正常
- [ ] 写个 `hello.c` 能编译运行
- [ ] `data/` 下 4 个文件就位，大小与上表一致

---

### 阶段 1：数据层 `data.c/h`

**目标**：把 MNIST 二进制文件读进内存，并能"看到"一张数字图。

**概念讲解点**：
- （讲） 二进制 vs 文本文件；为什么 MNIST 用紧凑二进制
- （讲） **大端字节序**（idx 的 magic 和维度都是大端，PC 是小端，读取要 `>> 24` 等手工拼字节）
- （讲） idx 格式：前 4 字节 magic（`0x00000803`=2051 表示图片、`0x00000801`=2049 表示标签），随后每维度 4 字节大端，最后是原始像素/标签字节
- （讲） 归一化：像素 `0–255` → `0.0–1.0`，为什么

**要写的代码**：
```c
// data.h
typedef struct {
    int n;                    // 样本数
    int h, w;                 // 28, 28
    float *images;            // [n][h*w]，归一化到 [0,1]
    unsigned char *labels;    // [n]，0~9
} MnistSet;

// 读取两个 idx 文件到一个集合；成功返回 0
int  mnist_load(MnistSet *set, const char *img_path, const char *lbl_path);
void mnist_free(MnistSet *set);

// 把第 idx 张图渲染成字符画，验证读取正确
void print_ascii_image(const float *img, int h, int w);
```
- `main.c` 先写一个临时入口：加载 train 集，打印第 0 号样本的字符画和标签。

**验收标准**：
- [ ] 运行程序能打印出**肉眼可辨认**的数字（如 5）字符画，并显示正确标签
- [ ] 样本数 = 60000 / 10000 打印正确
- [ ] 小郭能向 Claude 讲清楚：magic 怎么读、为什么像素要归一化

---

### 阶段 2：张量工具层 `tensor.c/h`

**目标**：定义全项目统一的张量结构，为神经网络铺路。

**概念讲解点**：
- （讲） 张量 = 多维数组；行优先连续内存（`data[i*C*H*W + c*H*W + h*W + w]`）
- （讲） 为什么用 4D `(N,C,H,W)`：一维（偏置/向量）、二维（权重矩阵）、三维（单样本多通道特征图）、四维（一批样本）都要装下
- （讲） `float` vs `double`：本项目用 `float`（内存小、和嵌入式更贴近），但梯度检查阈值要放宽

**要写的代码**：
```c
// tensor.h
typedef struct {
    float *data;     // 连续内存，行优先
    int ndim;        // 1~4
    int shape[4];    // 例如 {N,C,H,W}
    int size;        // 元素总数 = shape 各维乘积
} Tensor;

Tensor tensor_create(int ndim, const int shape[]);
void   tensor_free(Tensor *t);
void   tensor_fill(Tensor *t, float v);
// 任意维取值/取址宏（可选），以及 copy / reshape
```

**验收标准**：
- [ ] 写个小测试：创建一个 `{2,3,4}` 张量，逐元素写入再读出，打印验证正确
- [ ] 小郭能讲清楚 `size` 与 `shape` 的关系

> 建议：阶段 2 与阶段 3 的 Dense 层可以同步进行，用真实网络反向检验张量设计是否顺手。

---

### 阶段 3：MLP 热身网络

**目标**：实现一个两隐藏层的**最小可训练网络**，跑通"前向 → 反向 → SGD 更新 → 训练循环"全套流程，测试集准确率 ~92%。

**架构（锁定，2026-08-07 小郭改为两隐藏层）**：
```
输入(784) → Dense(128, ReLU) → Dense(64, ReLU) → Dense(10, Softmax)
参数量 ≈ 109,386（128×784 + 64×128 + 10×64 + 偏置），桌面 CPU 训练分钟级
```

**概念讲解点（教学主线，本阶段是重中之重）**：
1. 感知机 → 逻辑回归 → softmax 多分类的递进（从小郭会的线性回归讲起）
2. 为什么需要非线性激活函数（线性组合再多还是线性）
3. **前向传播**：数据如何逐层流动成预测
4. **损失函数**：交叉熵，以及 softmax+CE 联合梯度 `∂L/∂zᵢ = pᵢ − yᵢ` 的推导
5. **反向传播**：链式法则从损失倒推每一层梯度（配合 2×2 小网络手算）
6. SGD、batch、epoch、学习率；参数初始化

**要写的代码（实现顺序严格按此）**：
```c
// layer.h/c：先只有 Dense 层
typedef struct { /* 见附录 A 的 API 约定 */ } Layer;

// 1. dense_forward(Layer*, const Tensor *in, Tensor *out)   —— 含 ReLU 激活
// 2. 损失：softmax_crossentropy_loss(const Tensor *probs, const float *label_onehot)
//          + softmax_backward(...)  → 输出 δ = probs - onehot
// 3. dense_backward(Layer*, const Tensor *in, const Tensor *dz_next,
//                   Tensor *dout, Tensor *dw, Tensor *db)   —— 推链式法则
// 4. sgd_update(Layer*, float lr)                            —— w -= lr*dw
// 5. 训练循环 train.c / main.c train 模式：
//    - 打乱训练数据 → 每 batch(64) 前向+反向+更新
//    - 每 epoch 结束：打印 train loss、test 准确率
// 6. 数值梯度检查 check_gradients()（见 §5.3.6），专门模式跑通
```

**5.3.6 数值梯度检查（防呆核心，必做）**
- 原理：随机挑一层参数 `θ`，对每个参数做 `g_num = (L(θ+ε) − L(θ−ε)) / 2ε`（ε≈1e-4），与解析梯度 `g_ana` 对比。
- 判据：相对误差 `|g_num − g_ana| / max(|g_num|+|g_ana|, 1e-12) < 1e-4`（float 下）。
- 做法：每次实现一个新层，先用**单样本、小网络**（如 2-2-2）跑梯度检查，全绿再进下一层。
- 这个步骤能帮你把"公式推错"和"代码写错"在第一时间分开，**是 0 基础者的救命稻草**。

**训练参数（起跑参考，后面可调）**：
- 学习率 `η = 0.1`、batch = 64、epoch ≈ 10–20、权重初始化 `N(0, 0.01)`、偏置初始 0
- 预期：loss 逐 epoch 下降；测试集准确率爬到 90–93%

**常见坑（讲解要点）**：
- 权重全 0 / 初始化太大 → 不收敛或发散
- 学习率太大 → loss 震荡；太小 → 爬得极慢
- 忘了归一化输入 → 难收敛
- NaN/Inf 出现 → 优先检查 loss 与初始化

**验收标准**：
- [ ] 梯度检查通过（误差 < 1e-4）
- [ ] 训练 loss 明显下降，打印出每 epoch 的 train loss / test acc 表格
- [ ] 测试集准确率 ≥ 90%（92% 左右为达标线）
- [ ] 小郭能**用自己的话**讲清楚：一次前向做了什么、一次反向做了什么、`dw` 和 `db` 分别是什么

---

### 阶段 4：CNN

**目标**：在 MLP 骨架上加入卷积与池化，测试集准确率 ~97%+。

**架构（锁定）**：
```
Conv(1→8, 3×3, stride1, pad0) → ReLU → MaxPool(2×2)
→ Conv(8→16, 3×3) → ReLU → MaxPool(2×2)
→ Flatten → Dense(10) → Softmax
尺寸：28 → 26 → 13 → 11 → 5，Flatten 后 400 维
参数量 ≈ 5,258（conv1:80 + conv2:1168 + dense:4010），训练分钟级
```

**概念讲解点**：
1. 为什么全连接不够（空间结构被压平、参数爆炸）；卷积如何解决
2. 卷积 = 滑动窗口 + 权值共享；从图像滤波（sobel/边缘检测）直觉切入
3. **"卷积"实际是相关**（核不翻转）——先讲清楚这个易混点
4. stride / padding / 输出尺寸公式 `(H−K)/stride+1`
5. 特征图与多通道：`in_c → out_c` 每个输出通道一组核
6. 最大池化：降采样 + 平移不变性；为什么选 max
7. 感受野直觉
8. **卷积反向传播的两个方向**（重点推导，配 3×3 小例）：
   - `dW`：把 δ 当核、对输入做 valid 相关
   - `dInput`：把 δ 与翻转核做 full 相关（CS231n 式推导，耐心讲）
9. **最大池化反向传播**：梯度只回传给窗口内最大值位置

**要写的代码（顺序严格）**：
```c
// 1. conv_forward  —— 朴素三重循环（先不优化，性能阶段再谈 im2col）
// 2. maxpool_forward
// 3. conv_backward —— 先 dW、db，再 dInput（分开实现，分别梯度检查）
// 4. maxpool_backward
// 5. flatten 衔接：conv 输出 [C,H,W] → 一维向量给 Dense
// 6. 网络组装：把阶段 3 的 net/sgd/train 复用，替换前几层
// 7. 特征图可视化：把某层某通道输出打印成字符画，看到"轮廓被提取"
```

**每新增一个层，先单独过数值梯度检查再联调**（沿用 5.3.6）。

**训练参数（起跑参考）**：`η = 0.1`（可试 0.03）、batch = 64、epoch ≈ 10、初始化 `N(0, 0.1)`（He 直觉：对 ReLU 用 `sqrt(2/fan_in)`）。

**验收标准**：
- [ ] conv 层、pool 层各自的梯度检查全部通过
- [ ] 测试集准确率 ≥ 96%（97–98% 为达标线）
- [ ] 特征图字符画能看出网络在"找边缘/局部形状"
- [ ] 小郭能**推导 + 手算**一遍 3×3 例子的 `dInput` 怎么来

---

### 阶段 5：工程化与图片识别演示

**目标**：让模型可保存、可复用，并能识别一张真实的手写图片。

**概念讲解点**：
- （讲） 序列化与反序列化；自定义二进制模型格式（magic + 版本 + 各层 shape + 权重数据）
- （讲） 推理管线与训练解耦（demo 模式不碰训练代码）
- （讲） top-1 / top-3 与置信度（softmax 输出可解释为概率）
- （讲） 混淆矩阵与错误样本分析

**要写的代码**：
```c
// 1. net_save(net, "weights/cnn.bin") / net_load(net, "weights/cnn.bin")
// 2. main.c 三种模式：
//    train → 训练并存盘
//    eval  → 加载模型，打印测试集准确率 + 混淆矩阵 + 若干错误样本
//    demo  → 读取一张 28×28 灰度图（支持 PGM 最简；BMP 可选），
//            前向预测，输出 top-1 结果 + 各数字置信度
// 3. 提供一个"生成/获取手写图片"的最简途径：
//    程序内置一个 28×28 画板（终端输入坐标涂黑），或读一张自备 PGM
```

**验收标准**：
- [ ] `train` 训练后存盘；`eval` 重新加载后准确率与训练时一致
- [ ] `demo` 对测试集样本 / 自备手写图片预测正确，输出置信度
- [ ] 混淆矩阵能打印出来，小郭能读懂"哪个数字容易被认错"

---

### 阶段 6（可选延伸）

按兴趣和时间展开，以下任选：
- **性能优化**：im2col 把卷积转矩阵乘法、OpenMP 多线程
- **训练技巧**：momentum / 学习率调度 / dropout / 数据增强 / He 初始化
- **嵌入式延伸（贴合小郭背景）**：float→int8 定点量化、静态内存分配、裁剪推理-only 版本 → 尝试移植到 STM32

---

## 6. 侧边栏 Claude 指导守则

> 这一节是**给 Claude 的带教 SOP**，务必遵守。

### 6.1 三条核心职责

1. **代码编写辅助**：把思路落成 C 代码；解释每一处关键代码；排查编译/运行/数值错误。
2. **知识系统讲解**：所有概念从小郭的既有知识出发，系统化、按顺序讲。
3. **公式推导**：反向传播每个公式必须**从链式法则起步逐步推**，配 2×2/3×3 数值小例子手算核对。

### 6.2 讲解三步走模板（每个新概念都这样走）

1. **直觉**：一句话/一个类比（"卷积就是拿一个小模板在整个图上滑动，找相似"）。
2. **公式**：给出精确的数学形式（含维度标注），推导关键步骤。
3. **代码对照**：把公式的每一项对应到 C 代码的哪个循环/哪行，逐行读一遍。

然后**验证**：给一个极小例子（2×2 / 3×3），和小郭一起手算，再跑程序核对。

### 6.3 引导式提问（重要）

- ❌ 不要：小郭一问就甩 50 行完整代码。
- ✅ 应该：先问"你猜下一步该算什么？这个 δ 的形状应该是什么？"，等他思考 30 秒；卡住了给**一行提示**而非整段答案；他答对了再进入代码。

### 6.4 小步推进与验收

- 每次会话只推进一个小目标（一个函数 / 一个层 / 一个里程碑）。
- **每个新层实现完，先跑数值梯度检查**（§5.3.6）再继续。
- 每阶段对照验收标准，**达标才前进**；没达标就停下来排查，不要为了赶进度跳过。

### 6.5 代码风格约定（全项目统一）

- 纯 C11，只用标准库；`assert` 做维度/指针检查。
- 显式 `malloc` / `free`，每个 `create` 配一个 `free`；内存泄漏小郭自己 `-fsanitize` 也能查。
- 变量/函数名英文、清晰（`dense_forward`、`maxpool_backward`）；注释英文、解释"为什么"而非"是什么"。
- 无平台特定代码（不用 `#ifdef _WIN32` 写逻辑，必要时用 `fopen` 二进制模式 `"rb"` 等标准写法）。
- 每个文件头写明职责；头文件加 include guard。

### 6.6 讲解 vs 写代码的节奏判断

- 概念没懂 → **先讲**（走 6.2 模板）。
- 概念懂了、思路对但代码卡 → 帮他**逐行定位**，鼓励自己改。
- 概念和思路都对 → 放手让他写，Claude 做评审。

---

## 7. 常见坑与调试工具箱

### 7.1 数值陷阱

| 症状 | 常见原因 | 排查手段 |
|---|---|---|
| 出现 `nan` / `inf` | 学习率过大、初始化过大、log(0) | 打印 loss；初始化改小；softmax 用 `max` 平移技巧 `exp(z_i − z_max)` |
| loss 不降 | 学习率太小 / 忘了归一化 / 梯度算错 | 先跑梯度检查；试更大 lr；核对数据范围 |
| 训练震荡 | 学习率太大 | 调小 lr 或加 batch 大小 |
| 死 ReLU（某层输出全 0） | 初始化太大 | 用小随机初始化（He 直觉） |

### 7.2 工程陷阱

- **形状/索引错**：`assert(shape 匹配)` 加在每层前后；维度用 `%d` 打印核对。
- **大端读错**：`fread` 4 字节后用 `(b[0]<<24)|(b[1]<<16)|...` 拼，别直接存 int。
- **`fopen` 没开二进制模式**：Windows 下 `"r"` 会把 `0x1A` 当 EOF——**一定要 `"rb"`**。

### 7.3 调试工具箱（按优先级）

1. **数值梯度检查**：反向传播正确性的"金标准"，每层必跑。
2. **小数据集快速迭代**：先只喂 100–200 个样本跑几个 epoch，秒级验证"能不能过拟合"（能过拟合 = 代码基本对；连拟合都做不到 = 有 bug）。
3. **中间张量打印**：把某层前向输出 dump 出来看数值范围是否合理。
4. **`-Wall -Wextra` 编译** + `-fsanitize=address,undefined` 查内存问题（MinGW 支持）。
5. **loss 落盘成 csv**：每 epoch 输出一行 `loss,acc`，丢进 Excel/matplotlib 画曲线，直观看到收敛。

### 7.4 学习节奏参考（课余时间）

| 阶段 | 预计时间 |
|---|---|
| 0 | 半天–1天 |
| 1 | 半天 |
| 2 | 半天 |
| 3 | 2–3天 |
| 4 | 3–4天 |
| 5 | 1–2天 |
| **合计** | **约 1–2 周**（每天 1–2 小时） |

---

## 8. 参考资料

**视频（直觉最好，强烈推荐先看）**
- 3Blue1Brown《Neural networks》系列（神经网络+反向传播的几何直觉，配动画）：`https://www.3blue1brown.com/topics/neural-networks`
- 吴恩达《Deep Learning Specialization》第一、二门课（Coursera / B站搬运）

**课程（公式与推导最清楚）**
- Stanford **CS231n**（CNN 圣经，卷积反向传播讲得极清楚，有中文翻译版）：`https://cs231n.github.io/`
- CS231n 对 Python 不熟也没关系——**重点看它讲卷积/梯度推导的文字与图**，代码不用照抄

**书**
- 邱锡鹏《神经网络与深度学习》（开源中文书，公式推导友好）：`https://nndl.github.io/`
- Ian Goodfellow《深度学习》"花书"（作为字典查阅，不必通读）

**数据与工具**
- MNIST 官方页：`http://yann.lecun.com/exdb/mnist/`；镜像：`https://ossci-datasets.s3.amazonaws.com/mnist/`
- 数值梯度检查：CS231n assignment 2 的 `gradient_check.py` 思路（本项目用 C 实现同样的算法）

---

## 附录 A：项目目录结构与技术决策

### A.1 目录结构（锁定）

```
Handwritten_digit_recognition/
├── GUIDE.md              # 本文档
├── Makefile              # gcc *.c -O2 -o main.exe 的封装
├── data/                 # MNIST 数据集（idx 文件）
├── weights/              # 训练好的模型权重（阶段5起生成）
├── scripts/              # 数据集下载脚本（PowerShell / Git Bash）
└── src/
    ├── main.c            # 入口：train / eval / demo 三种模式
    ├── tensor.c / .h     # Tensor 结构体与基础运算
    ├── data.c / .h       # MNIST 读取
    ├── layer.c / .h      # Dense / Conv / MaxPool 层（前向+反向+参数+梯度检查）
    ├── net.c / .h        # 网络组装、前向/反向/更新
    ├── sgd.c / .h        # 优化器（SGD；阶段6 可加 momentum）
    └── predict.c / .h    # 推理、混淆矩阵、图片识别演示
```

### A.2 关键技术决策（Claude 全项目保持一致）

| 决策 | 取值 | 理由 |
|---|---|---|
| 语言标准 | C11，仅标准库 | 零依赖、可移植 |
| 编译器 | MinGW-w64 GCC | 免费、VSCode 集成好、贴近小郭的 GCC-ARM 经验 |
| 构建 | 简单 Makefile | 一眼看懂，不引入 CMake 复杂度 |
| 张量 | `Tensor{float*, shape[4], ndim, size}`，行优先连续内存 | 全项目统一，为嵌入式预留 |
| 精度 | `float` | 内存小、贴近嵌入式；梯度检查阈值放宽到 1e-4 |
| 权重初始化 | MLP: `N(0,0.01)`；CNN 卷积: He 直觉 `sqrt(2/fan_in)` | 训练稳定的起跑配置 |
| 反传验证 | 每层先过**数值梯度检查** | 本项目最重要的防呆机制 |
| 随机性 | 用 `rand()` + 固定 seed 便于复现 | 调试友好 |

### A.3 layer.h API 约定（给 Claude 的实现基准）

```c
typedef enum {
    LAYER_DENSE, LAYER_RELU, LAYER_SOFTMAX,
    LAYER_CONV, LAYER_MAXPOOL, LAYER_FLATTEN
} LayerType;

typedef struct {
    LayerType type;
    // Dense
    int in_size, out_size;
    float *w, *b, *dw, *db;            // [out][in] / [out]
    // Conv
    int in_c, out_c, kh, kw, stride, pad;
    float *cw, *cb, *dcw, *dcb;        // [out_c][in_c][kh][kw] / [out_c]
    // MaxPool
    int pool_h, pool_w;
    // 缓存：前向结果 / 反向输入梯度
    Tensor in, out, dz;
} Layer;

// 每个层两函数：forward 存缓存供 backward 用
void layer_forward(Layer *l, const Tensor *in, Tensor *out);
void layer_backward(Layer *l, const Tensor *dout, Tensor *din);
// 更新参数
void layer_update(Layer *l, float lr);
```

> 说明：上面的结构体是"约定"，允许 Claude 与小郭在实现中优化（如把激活合并进 Dense 层、用 `Layer` 数组 + `Net` 封装），但**张量表示、数值梯度检查、验收标准这三样不能变**。

---

## 附录 B：进度打卡表

> 每完成一项打 ✅。Claude 会话开始时对照此表。

| 阶段 | 任务 | 状态 |
|---|---|---|
| 0 | 工具链就绪、数据文件就位 | ✅ 2026-08-03 |
| 1 | 能打印 MNIST 数字字符画 | ✅ 2026-08-04 |
| 2 | Tensor 结构与自测通过 | ✅ 2026-08-05 |
| 3 | MLP 梯度检查通过 | ☐ |
| 3 | MLP 测试集 ~92% | ☐ |
| 4 | Conv/Pool 梯度检查通过 | ☐ |
| 4 | CNN 测试集 ~97%+ | ☐ |
| 5 | 权重保存/加载可用 | ☐ |
| 5 | demo 读图预测正确 | ☐ |

> **2026-08-03 会话备注（阶段 1 启动）**
> - 阶段 0 全部验收通过：4 个数据文件大小正确、gcc 15.2.0、mingw32-make 4.4.1
> - 已建立 Git 跟踪，完成首次提交 `49fd51a`；src/main.c 当前为大小端 union 实验代码（学习留痕）
> - 已学阶段 1 概念：二进制 vs 文本、magic number 与 idx 格式、大端字节序（含 union 实验）、printf 格式占位符
> - **下一步**：实现 `data.c/h`（`mnist_load` / `print_ascii_image`），打印出可辨认的数字字符画

> **2026-08-04 会话备注（阶段 1 完成）**
> - `data.c/h` 三个函数全部实现：`mnist_load`（goto cleanup + `success` 标志）、`mnist_free`、`print_ascii_image`
> - 踩并修通两个隐蔽 bug：
>   1. goto cleanup 成功路径误 free 数据 → SIGSEGV；加 `success` 标志，**只有失败才释放数据**（成功时数据归调用者）
>   2. 标签 idx 文件漏读 count 4 字节 → 标签整体错位（第 0 张图是 5 却打印 label=0）；补读 count 并校验与图像样本数一致
> - 验证通过：train 60000 / test 10000；字符画可辨认（train 前 5 张 5,0,4,1,9；test 前 3 张 7,2,1）；标签与图像配对正确
> - 阶段 1 概念验收通过：小郭能讲清大端拼字节（含"单字节像素无字节序问题"的洞见）与归一化动机（量级匹配）
> - **下一步**：阶段 2 张量工具层 `tensor.c/h`

> **2026-08-05 会话备注（阶段 2 完成）**
> - `tensor.c/h` 三件套实现：`tensor_create`（goto cleanup 练习 + 参数校验 + size 计算 + malloc）、`tensor_free`、`tensor_fill`（扁平遍历，一层循环）
> - 踩坑记录：`nidm` 拼写、`size*=ndim` 多乘 3 倍、`==` 写反成 `=`、cleanup 条件写反（成功路径误 free 数据，重演阶段 1 SIGSEGV 风险）；修复后 cleanup 只 `return err`，malloc 前先置 `t->data=NULL`
> - 关键领悟：**连续内存 → fill 不需要按维度嵌套循环，扁平遍历即可**；shape（逻辑形状）与 size（扁平大小）是同一张量的两种描述
> - 验证通过：`{2,3,4}` 张量 size=24，逐元素验证 `All elements OK`
> - **下一步**：阶段 3 MLP 热身网络——先讲"单个神经元 = 线性回归 `ŷ=wx+b` + 梯度下降"，再实现 Dense 层（详见 §5 阶段 3 与 §4.3 概念清单，注意归一化回扣点）

> **2026-08-06 会话备注（阶段 3 代码启动）**
> - 概念课完成并博客留档：`D:\Hexo\source\_posts\2026-08-06-gradient-descent-experiment.md`（收敛→发散→η按x²缩小恢复→归一化动机；实验程序提交 `7084572`）
> - **下一步**：带教模式写 `dense_forward`——Claude 只引导不代写（GUIDE §2.2 守则），先提问让小郭想清"一层神经元"的权重形状与内存布局，再自己写 `layer.h/c` 并手算验收

> **2026-08-07 会话备注（阶段 3：dense_forward + ReLU 完成）**
> - 架构改为两隐藏层：784→Dense(128,ReLU)→Dense(64,ReLU)→Dense(10,Softmax)（已更新本文件锁定架构）
> - 小郭独立实现：`layer.h/c` 的 `dense_forward`（两层循环 + 行优先下标 `W[i*col+j]`）、`relu_activation`（就地 `max(0,x)`）、`relu_forward`（复合封装：dense+激活）
> - 设计亮点：激活原语 `relu_activation` 与复合层 `relu_forward` 分层（"便捷封装 + 底层原语"，灵感来自 STM32 标准库接口风格）
> - 踩坑记录：`Tensor *` 访问数据漏 `->data`（编译器不报、运行时越界）；内层循环结束后用 `j` 索引外层数组（越界）；`result` 用 `int`（应 `float`）；NULL 检查放在解引用之后（应先查 NULL）；`relu_activation` 过度校验与自身无关的字段（`weight/bias` 等）
> - 验收通过：手算 `-0.8 / 0.4`，ReLU 压负留正，与程序输出一致
> - **下一步**：softmax + 交叉熵损失（`softmax_forward` / `softmax_crossentropy_loss`），联合梯度 `∂L/∂z = p − y`

> **2026-08-15/16 会话备注（阶段 3：反向传播闭环）**
> - 反向四件套全部完成并手算验收：`softmax_backward`（δ = p − y，联合梯度省去 softmax 雅可比）、`dense_backward`（gradW=δ⊗a_prev 外积 / gradB=δ / 回传 δ_prev=Wᵀδ，索引转置不物理搬移）、`relu_backward`（δz=δa⊙relu'(z)，判开关只读 activation_value）、`sgd_update`（w-=lr·gradW，含 lr<=0 防御）
> - 关键架构决策（方案 B）：每层加 `delta` 缓冲承载误差信号，`activation_value` 永远只存激活值、backward 只读。曾试过覆写 actval 存 0/1 开关，虽数学正确但破坏激活值信息、引入顺序依赖，已修正为只读判开关
> - 踩坑记录：dense_backward 曾用 `float** transpose = malloc(col*row*sizeof(float))` 解引用未初始化垃圾指针崩溃 → 弃用物理转置，改"固定列 j、遍历行 i 的跳步读取"即得 Wᵀ 效果（小郭已理解）
> - 验证通过：gradW/gradB/prev_delta/relu 开关/sgd 更新后参数，全部与手算一致；relu 测试覆盖正/负/恰零三种情况
> - **下一步**：搭训练循环——权重随机初始化（不能从 0 开始）→ 组装三层(128/64/10) → 接阶段 2 data 层 → epoch/sample/loss/acc 打印

---

*本文档由 Claude 与小郭共同维护。每到一个里程碑，可更新进度打卡表与"学到的新知识"备注。*
